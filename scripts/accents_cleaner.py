import pandas as pd
import numpy as np


class AccentsCleaner:
    """A class to clean dataframes from any accents"""

    def __init__(self, dataframes: pd.DataFrame) -> pd.DataFrame:
        pass

    def __repr__(self) -> str:
        pass

    # go over each df -> look a accents -> replace the accents with english equivalent
